# Skip building optional dependencies
ignore-optional=true

# Disable audit to speed up installation
audit=false

# Disable fund messages
fund=false

# Increase network timeout for slow connections
network-timeout=60000

# Disable progress bar for cleaner logs
progress=false

# Legacy peer dependencies mode for compatibility
legacy-peer-deps=true